modLoader="javafml" #mandatory
loaderVersion="[36,)" #mandatory This is typically bumped every Minecraft version by Forge. See our download page for lists of versions.
issueTrackerURL="https://github.com/Alex-the-666/local-looks/issues" #optional
license="GNU LESSER GENERAL PUBLIC LICENSE"
[[mods]] #mandatory
modId="locallooks" #mandatory
version="1.2.1" #mandatory
displayName="Local Looks" #mandatory
displayURL="https://www.curseforge.com/minecraft/mc-mods/local-looks" #optional
authors="Alexthe666" #optional
description='''Local Looks adds a magic mirror which allows players to change their skin for every world.'''
[[dependencies.locallooks]] #optional
    # the modid of the dependency
    modId="citadel" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[1.10.0,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="AFTER"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"